name: 提供AI提问提示词
on:
  # 触发条件1：新建Issue（可选保留）
  issues:
    types: [opened]
  # 触发条件2：新增Issue评论（核心，检测@issue_bot_cn）
  issue_comment:
    types: [created]

jobs:
  provide-ai-prompt:
    runs-on: ubuntu-latest
    permissions:
      issues: write  # 评论Issue权限
      contents: read # 读取仓库内容权限
    # 仅满足以下条件时执行：评论含@issue_bot_cn 或 新建Issue（可选）
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@issue_bot_cn')) ||
      (github.event_name == 'issues' && github.event.action == 'opened')
    steps:
      - name: 拉取仓库代码
        uses: actions/checkout@v4

      - name: 生成并发布AI提问提示词
        uses: actions/github-script@v7
        with:
          script: |
            // 获取当前Issue信息
            const issueNumber = context.issue.number;
            const issueTitle = context.payload.issue.title;
            const issueBody = context.payload.issue.body;
            const issueLink = `https://github.com/${context.repo.owner}/${context.repo.repo}/issues/${issueNumber}`;
            const docLink = 'https://ais-bench-benchmark-rf.readthedocs.io/';
            const historicalIssues = `https://github.com/${context.repo.owner}/${context.repo.repo}/issues`;

            // 生成提示词
            const prompt = `你现在需要解决一个关于 AISBench/benchmark 仓库的issue，请参考历史相关Issue和官方文档，并提供此issue的解决方案：\n\n【当前issue】\nIssue 链接：${issueLink}\n标题：${issueTitle}\n\n【历史相关Issue参考】\n${historicalIssues}\n\n【官方文档】\n${docLink}\n\n请结合以上所有信息，提供完整的解决方案或建议。`;

            // 生成AI助手链接列表
            const aiAgents = `\n\n常用AI助手链接：\n- 豆包：https://www.doubao.com/\n- ChatGPT：https://chat.openai.com/\n- 文心一言：https://yiyan.baidu.com/\n- 通义千问：https://tongyi.aliyun.com/`;

            const furtherHelp = `\n\n如果以上信息无法解决你的问题，可以在 AISBench/benchmark 仓库的 Issue 中继续讨论，或者联系仓库维护者（@SJTUyh）。`
            // 发布评论
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issueNumber,
              body: `🤖 为了更好地帮助你解决问题，我们为你准备了 AI 提问提示词。你可以复制以下内容到任意 AI 助手中（如豆包、ChatGPT 等）获取专业解答：\n\n\`\`\`\n${prompt}\n\`\`\`${aiAgents}${furtherHelp}`
            });